<Application x:Class="Publishing.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Publishing"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Accent/MaterialDesignColor.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>
        

            <!-- Для выезжания и заезжания меню при наведении мышки -->
            <Style x:Key="spSlideLeft" TargetType="StackPanel">

                <Style.Triggers>

                    <EventTrigger RoutedEvent="MouseEnter">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="-240,0,0,0" To="0, 0, 0, 0" DecelerationRatio=".9" Duration="0:0:0.8"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>

                    <EventTrigger RoutedEvent="MouseLeave">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,0,0,0" To="-240, 0, 0, 0" DecelerationRatio=".9" Duration="0:0:0.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>

                </Style.Triggers>

            </Style>

            <!-- Для подсвечивания надписей меню при наведении курсора -->
            <Style x:Key="MenuLabel" TargetType="Label">
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="Padding" Value="15,0,0,0"/>
                <Setter Property="Foreground" Value="#FF69B4"/>
                <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
                <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="#E4007C"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontWeight" Value="Bold"/>

                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="False">
                        <Setter Property="Foreground" Value="#FF69B4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Рамка вокруг элементов меню при наведении курсора -->
            <Style x:Key="borderEdge" TargetType="Border">
                <Setter Property="CornerRadius" Value="20"/>
                <Setter Property="BorderThickness" Value="3.5"/>
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Setter Property="Margin" Value="2"/>

                <Style.Triggers>

                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="BorderBrush" Value="#E4007C"/>                        
                    </Trigger>

                    <Trigger Property="IsMouseOver" Value="false">
                        <Setter Property="BorderBrush" Value="{x:Null}"/>
                    </Trigger>

                </Style.Triggers>
            </Style>

            <!-- Stile for Line -->
            <!-- {x:Type Line} - для всех подобных эл-тов в сборке -->
            <Style x:Key="changeLine" TargetType="Line">
                <Setter Property="Margin" Value="5,0,65,0"/>
                <Setter Property="StrokeThickness" Value="5"/>
                <Setter Property="X2" Value="233"/>
                <Setter Property="Height" Value="2"/>
                <Setter Property="Stroke" Value="#FF69B4"/>

                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=STP1}" Value="True">
                        <Setter Property="Stroke" Value="#E4007C"/>
                        <Setter Property="StrokeThickness" Value="7"/>
                        <Setter Property="Height" Value="4"/>
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard Storyboard.TargetProperty="X2">
                                    <DoubleAnimation To="293" Duration="0:0:0.1"/>
                                </Storyboard>
                            </BeginStoryboard>
                            <BeginStoryboard>
                                <Storyboard Storyboard.TargetProperty="Margin">
                                    <ThicknessAnimation To="5,0,5,10" Duration="0:0:0.1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>

                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard Storyboard.TargetProperty="X2">
                                    <DoubleAnimation To="233" Duration="0:0:0.1"/>
                                </Storyboard>
                            </BeginStoryboard>
                            <BeginStoryboard>
                                <Storyboard Storyboard.TargetProperty="Margin">
                                    <ThicknessAnimation To="5,0,65,10" Duration="0:0:0.1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>

                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- I dont know is it rly need? -->
            <!--<Style x:Key="changeGrid" TargetType="Grid">
                <Setter Property="Margin" Value="65,10,10,10"/>
                <Style.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard >
                                    <ThicknessAnimation Storyboard.TargetProperty="Margin" To="300, 100, 100, 100" Duration="0:0:1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>                        
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard >
                                    <ThicknessAnimation Storyboard.TargetProperty="Margin" To="65,10,10,10" Duration="0:0:1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>
                </Style.Triggers>                
            </Style>-->
                
                
                
            
        </ResourceDictionary>

    </Application.Resources>
</Application>
<!-- Grey #FFACB5B9 (start), #FFC826, Oliva #808000, Fuchsia #FF00FF, PaleVioletRed #DB7093, HotPink #FF69B4, DeepPink #FF1493, MediumVioletRed #C71585, DarkGoldenRod #B8860B, 
    Goldenrod #DAA520, Gold #FFD700, Pure Gold #D3AD7E, Mexican Pink #E4007C -->

<!--#FF1493  changed in Mexican Pink #E4007C -->